const styles = {
  zIndex: "2000",
  position: "fixed",
  top: 0,
  right: 0,
  left: 0,
  bottom: 0,
  backgroundColor: "#00000080",
};

const Blocker = () => {
  return <div style={styles}></div>;
};

export default Blocker;
